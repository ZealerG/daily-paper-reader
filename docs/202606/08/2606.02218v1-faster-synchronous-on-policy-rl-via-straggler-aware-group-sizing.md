---
title: Faster Synchronous On-Policy RL via Straggler-Aware Group Sizing
title_zh: 通过感知拖后腿者的组大小调整实现更快速的同步在策略强化学习
authors: "Azal Ahmad Khan, Ammar Ahmed, Zeshan Fayyaz, Sheng Di, Mingyi Hong, Ali Anwar"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02218v1"
tags: ["query:graph-part"]
score: 9.0
evidence: 针对同步在线强化学习的落后感知组大小控制
tldr: 同步在线策略强化学习如GRPO易受掉队者影响，组增大导致同步延迟。本文提出SAGC，在线动态调整组大小，将组选择建模为约束优化问题以控制掉队率。在GRPO和DAPO训练中，SAGC减少了掉队事件，提高了墙钟效率，并在推理基准上达到或优于静态组大小基线，且输出更短。动态组控制使同步强化学习更高效鲁棒。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02218v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02218v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1624, \"height\": 785, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02218v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 545, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02218v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 668, \"height\": 442, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02218v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 811, \"height\": 939, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02218v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 811, \"height\": 684, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02218v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 810, \"height\": 683, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02218v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 748, \"height\": 232, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02218v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1185, \"height\": 274, \"label\": \"Table\"}]"
motivation: 同步策略梯度方法中，单个长推演（掉队者）会导致全组更新延迟，且随组规模增大而恶化，需解决效率与组大小的矛盾。
method: 提出SAGC，在线将组大小选择建模为约束优化问题，基于推演行为动态调整，以保留大组优势同时控制长期掉队率。
result: 在GRPO和DAPO训练中，SAGC持续降低掉队事件，提升墙钟效率，最终模型在推理基准上表现竞争或更优，且输出更短。
conclusion: 动态组控制是一种实用方法，可使同步在线策略强化学习更高效、鲁棒，兼顾训练速度与模型质量。
---

## 摘要
同步强化学习方法（如组相对策略优化，GRPO）提供了稳定且可复现的在策略训练，但它们极易受到掉队者的影响——单个异常长的 rollout 可能会延迟整个组的奖励计算和参数更新。随着组规模的增大，这一问题变得更加严重，从而在更大组的优势与同步停滞的挂钟时间成本之间产生矛盾。我们提出了一种动态组大小控制器——感知拖后腿者的组控制（SAGC），它能根据观测到的 rollout 行为在线调整训练组。SAGC 将组大小选择形式化为一个在线约束优化问题，旨在保留更大组的优势，同时控制掉队事件的长期发生率。在同步 GRPO 和 DAPO 训练中，以及在原始和强工程化的基线之上，SAGC 持续降低了掉队发生率，提高了挂钟效率，同时实现了具有竞争力或更好的训练奖励。我们进一步表明，这些收益会转化为最终的模型质量：在下游推理基准测试中，SAGC 与最强的静态组大小基线相比具有竞争力或更优，并且通常能生成更短的输出而无需任何显式的长度惩罚。这些结果将动态组控制定位为使同步在策略强化学习更高效和更鲁棒的实用方法。

## Abstract
Synchronous reinforcement learning methods such as Group Relative Policy Optimization (GRPO) provide stable and reproducible on-policy training, but they are highly vulnerable to stragglers, a single unusually long rollout can delay reward computation and parameter updates for the entire group. This problem becomes more severe as group size increases, creating a tension between the benefits of larger groups and the wall-clock cost of synchronization stalls. We propose Straggler-Aware Group Control (SAGC), a dynamic group-size controller that adapts the training group online based on observed rollout behavior. SAGC formulates group-size selection as an online constrained optimization problem, seeking to retain the benefits of larger groups while controlling the long-term rate of straggler events. Across synchronous GRPO and DAPO training, and on top of both vanilla and strong engineered baselines, SAGC consistently reduces straggler incidence and improves wall-clock efficiency while achieving competitive or better training reward. We further show that these gains transfer to final model quality: SAGC is competitive with or better than the strongest static group-size baseline on downstream reasoning benchmarks, and often produces shorter outputs without any explicit length penalty. These results position dynamic group control as a practical way to make synchronous on-policy RL more efficient and robust.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：同步在线策略强化学习方法（如GRPO、DAPO）虽能提供稳定、可复现的训练，但存在严重的掉队者问题——单个异常长的 rollout 会延迟全组的奖励计算和参数更新。随着组规模增大，出现极端长度 rollouts 的概率升高，导致大量 GPU 空闲等待时间（straggler-induced stalls）。
- **核心矛盾**：更大的组能提升优势估计的统计质量（更多组内比较），但会加剧同步停滞的墙钟成本。现有系统优化（如连续批处理）无法消除组级依赖；异步方法虽提高利用率，却引入策略滞后和训练一致性损失。
- **整体含义**：需要一种保留同步严格在策略优势的同时，降低掉队者成本的方法。本文提出动态组大小控制，将组大小从静态超参数转化为在线自适应问题，以平衡统计效率与计算效率。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将组大小选择建模为在线约束优化问题，目标是最大化组大小带来的效用（如 $\log_2 G$），同时控制长期掉队率在目标阈值 $\delta$ 以下。
- **关键技术细节**：
  - **掉队事件定义**：基于组内最大响应长度与中位数长度的比值 $R = \max \ell_i / \text{median} \ell_i$，设定阈值 $\tau$，当 $R > \tau$ 时视为掉队事件 $S=1$。
  - **在线拉格朗日方法**：引入拉格朗日乘子 $\lambda_t$，每步选择最大化 $u(G) - \lambda_t \hat{p}_t(G)$ 的组大小，其中 $\hat{p}_t(G)$ 是掉队概率估计。乘子通过投影梯度上升更新：$\lambda_{t+1} = [\lambda_t + \eta_\lambda (\bar{S} - \delta)]_+$。
  - **掉队概率估计**：采用贝叶斯方法，为每组大小维护 Beta 先验，观测后更新后验，并通过 Thompson 采样从后验抽样得到 $\tilde{p}_G$，以平衡探索与利用。
  - **局部搜索约束**：为避免振荡，每次只允许组大小在相邻值中选择（如 $G \in \{4,8,16\}$ 内）。
  - **系统实现**：控制器运行在 CPU 端，GPU 只传递长度摘要统计，通信开销可忽略。
- **算法流程**：初始化 $\lambda=0$、各 $G$ 的 Beta(1,1)；每步执行 rollout 生成，收集组长度统计，计算经验掉队率 $\bar{S}$，更新 $\lambda$ 和后验参数，采样 $\tilde{p}_G$，在局部邻域中选择使 $u(G)-\lambda \tilde{p}_G$ 最大的 $G$。

## 3. 实验设计

- **模型与算法**：使用 Qwen2.5-3B-Instruct 和 Llama-3.2-3B-Instruct 两个基础模型；训练算法包括 GRPO 和 DAPO。
- **数据集**：训练集来自 open-r1/DAPO-Math-17k-Processed（数学推理 prompts）；评估使用 AIME 2023-2025、AMC 2023、GPQA Diamond 五个下游推理基准。
- **对比方法**：
  - 静态组大小基线：固定 $G \in \{4,8,16\}$，构建在两种系统实现上：
    - Vanilla：最小优化同步实现。
    - OpenRLHF：强工程化基线（vLLM + DeepSpeed）。
  - 动态组大小：SAGC（基于 OpenRLHF）。
- **公平性设置**：固定有效生成批大小 $EB=64$（$EB = B \times G$），即每步生成总 rollout 数相同；静态基线中 $G=4,8,16$ 分别对应 $B=16,8,4$；SAGC 在线调整 $B_t = 64/G_t$。这样保证了每步生成工作量相同，墙钟节省必须来自更好的执行调度。

## 4. 资源与算力

- 所有训练在单张 NVIDIA H200 GPU（141GB 内存）上完成。
- 总训练和评估消耗约 1000 H200 GPU 小时，折合超过 2400 美元计算费用。
- 共运行 28 种训练配置（2 模型 × 2 算法 × 多个静态组大小 + SAGC），加上下游评估。

## 5. 实验数量与充分性

- **实验规模**：覆盖 2 个模型族、2 种算法、4-5 种组大小配置（包括静态和动态），共 28 组训练。评估包含 5 个下游基准（AIME x3、AMC、GPQA），每个检查点生成 pass@4 准确率。
- **充分性**：实验设计较充分——对比了两种系统基线（Vanilla vs. OpenRLHF）以区分系统优化与动态控制的效果；固定有效批大小确保公平；包含训练指标（时间、掉队率、奖励、长度）和下游指标（准确率、响应长度）。但缺乏对更大模型（如 7B/13B）和多 GPU 分布式训练的扩展实验；未做消融实验（如不同 $\tau$、$\delta$ 的影响）。
- **客观性**：公平性设置合理；结果呈现清晰（表格标注最好/次优），未隐藏不利结果（如 Llama GRPO 下 SAGC 训练奖励不如 G=16 静态基线但时间显著更短）。

## 6. 主要结论与发现

- **训练效率**：SAGC 在所有配置下均显著降低掉队率（例如 Llama DAPO 从 99.6% 降至 43.4%），训练时间接近或优于同奖励水平的静态组大小（如 Qwen GRPO 下 SAGC 7.93h vs. G=8 的 7.77h，但奖励更高 0.648 vs. 0.594）。
- **下游性能**：SAGC 在 AIME、AMC、GPQA 上通常达到最好或次好成绩，尤其在 DAPO 下全面领先；GRPO 下接近最好静态基线。
- **输出长度**：SAGC 往往产生更短响应（无显式长度惩罚），暗示动态控制隐式抑制了过度生成。
- **后验风险分离**：贝叶斯估计快速识别出大组掉队风险更高（$\hat{p}_{16} > \hat{p}_8 > \hat{p}_4$），且模型间差异（Llama 风险高于 Qwen）被自动捕获。

## 7. 优点

- **方法简洁实用**：SAGC 仅需长度统计，无需梯度或奖励信息，CPU 侧运行，可作为 GRPO/DAPO 管线的即插即用组件。
- **创新思路**：将组大小选择转化为在线约束优化问题，利用贝叶斯后验和 Thompson 采样实现自适应控制，兼顾掉队率约束和效用最大化。
- **实验设计公平**：固定有效批大小消除生成量差异，区分系统优化（Vanilla vs. OpenRLHF）与动态控制（OpenRLHF vs. SAGC）的贡献。
- **结果全面**：同时报告训练指标和下游指标，验证了系统收益向模型质量的转化；发现隐式长度缩短现象，提供了额外见解。
- **鲁棒性**：在两个模型族、两种算法上一致有效，不依赖特定实现。

## 8. 不足与局限

- **实验覆盖有限**：仅测试 3B 级模型，未评估更大模型（7B/13B/70B）或多 GPU 并行场景；未在分布式训练中验证通信开销是否会抵消收益。
- **消融实验缺失**：未系统分析 $\tau$（掉队阈值）、$\delta$（目标率）等超参数的影响；未比较其他自适应策略（如基于比例的启发式）。
- **控制器简化**：忽略 prompt 类型或难度信号，无法提前识别易掉队查询；未来可考虑混合 prompt 感知先验。
- **算法依赖性**：虽声称算法无关，但仅验证 GRPO 和 DAPO，未扩展至其他同步算法（如 PPO with critic）。
- **潜在偏差**：训练数据集为数学推理（DAPO-Math），下游评估主要为数学/科学，结论向其他领域（如代码、对话）的泛化性未知。
- **隐式长度惩罚可解释性**：缩短输出的机制未深入分析，可能源于训练分布改变而非有意约束，需更多理论支持。

（完）
