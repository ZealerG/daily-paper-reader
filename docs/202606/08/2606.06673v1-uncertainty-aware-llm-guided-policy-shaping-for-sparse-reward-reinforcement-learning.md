---
title: Uncertainty-Aware LLM-Guided Policy Shaping for Sparse-Reward Reinforcement Learning
title_zh: 面向稀疏奖励强化学习的、具不确定性感知的大语言模型引导策略塑形
authors: "Ujjwal Bhatta, Utsabi Dangol, Sumaly Bajracharya, Rodrigue Rizk, KC Santosh"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06673v1"
tags: ["query:graph-part"]
score: 9.0
evidence: 大语言模型引导的策略塑形用于稀疏奖励强化学习
tldr: "稀疏奖励和异构任务序列导致强化学习收敛慢、泛化差、探索低效。提出ULPS框架，利用A*最优符号轨迹微调BERT语言模型，通过MC dropout估计认知不确定性，用熵基混合机制自适应平衡LLM引导和PPO策略。在MiniGridUnlockPickup上，成功率、奖励效率和样本复杂度均优于基线，执行准确率提升超过9%。为稀疏奖励多任务强化学习提供结合符号规划、语言先验和不确定性感知控制的有效方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06673v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06673v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 897, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06673v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06673v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 727, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06673v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1477, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06673v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 543, \"height\": 362, \"label\": \"Table\"}]"
motivation: 解决稀疏奖励和异构任务序列下强化学习收敛慢、泛化弱、探索效率低的问题。
method: "利用A* oracle生成最优符号轨迹微调BERT，通过MC dropout估计认知不确定性，用熵基混合机制自适应平衡LLM引导与PPO策略。"
result: "在MiniGridUnlockPickup上，ULPS相比基线成功率、奖励效率和样本复杂度显著提升，执行准确率提高超过9%。"
conclusion: 整合符号轨迹、语言先验和不确定性感知控制，为稀疏奖励多任务强化学习提供高效可扩展的框架。
---

## 摘要
稀疏奖励和异构任务序列仍然是强化学习（RL）中持续的挑战，常导致收敛缓慢、泛化能力弱和探索效率低下。我们提出了一种新颖框架——具不确定性感知的大语言模型引导策略塑形（ULPS），该框架将经过校准的大语言模型（LLM）集成到RL训练循环中，以提供结构化、不确定性调节的行为引导。ULPS采用基于A*的预言器合成最优符号轨迹，用于微调基于BERT的语言模型。在训练过程中，该模型提供动作建议，其影响取决于通过蒙特卡洛（MC）dropout估计的认知不确定性。一种基于熵的混合机制自适应地平衡LLM引导和学习策略（通过近端策略优化，PPO），使智能体能够在优先考虑可靠先验的同时保持适应性。我们在MiniGridUnlockPickup基准上评估了ULPS，并观察到与无引导、未校准和标准RL基线相比，在成功率、奖励效率和样本复杂度方面的一致改进。ULPS在微调后执行准确率提高了超过9%，需要更少的环境交互，并获得了更高的奖励AUC。我们的结果表明，集成符号A*轨迹、预训练语言先验和不确定性感知控制为稀疏奖励领域中的多任务强化学习提供了一种有原则且有效的方法，并具有扩展到部分可观测和多智能体设置的潜力。

## Abstract
Sparse rewards and heterogeneous task sequences remain persistent challenges in Reinforcement Learning (RL), often resulting in slow convergence, weak generalization, and inefficient exploration. We propose Uncertainty-Aware LLM-Guided Policy Shaping (ULPS), a novel framework that integrates a calibrated Large Language Model (LLM) into the RL training loop to provide structured, uncertainty-modulated behavioral guidance. ULPS employs an A*-based oracle to synthesize optimal symbolic trajectories, which are used to fine-tune a BERT-based language model. During training, this model supplies action suggestions whose influence is conditioned on epistemic uncertainty estimated via Monte Carlo (MC) dropout. An entropy-based blending mechanism adaptively balances LLM guidance and the learned policy (via Proximal Policy Optimization, PPO), allowing the agent to prioritize reliable priors while preserving adaptability. We evaluate ULPS on the MiniGridUnlockPickup benchmark and observe consistent improvements in success rate, reward efficiency, and sample complexity over unguided, uncalibrated, and standard RL baselines. ULPS achieves more than 9% improvement in execution accuracy after fine-tuning, requires fewer environment interactions, and yields higher reward AUC. Our results demonstrate that integrating symbolic A* trajectories, pretrained language priors, and uncertainty-aware control offers a principled and effective approach to multi-task reinforcement learning in sparse-reward domains, with potential extensibility to partially observable and multi-agent settings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在强化学习（RL）中，稀疏奖励和异构任务序列导致智能体探索效率极低、收敛缓慢以及泛化能力弱。传统RL方法（如PPO、DQN）在稀疏奖励环境中往往需要大量无意义的随机探索，无法有效利用环境的结构信息。
- **研究背景**：近年来，大型语言模型（LLM）在推理和规划方面展现出潜力，但直接将其策略注入RL会导致对不可靠建议的过度依赖，甚至因LLM的过度自信与幻觉而损害训练稳定性。因此，亟需一种能估计LLM不确定性并据此调节其影响的方法。
- **整体含义**：本文提出的ULPS框架将经过校准的LLM（基于A*最优轨迹微调的BERT）集成到PPO训练循环中，通过MC dropout估计认知不确定性，并利用熵基混合机制自适应平衡LLM引导与策略学习，旨在同时提升稀疏奖励环境下的样本效率和最终性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用A*搜索生成最优符号轨迹微调BERT语言模型，使其能产生与专家一致的动作建议；在RL训练时，通过MC dropout获得动作概率分布及其不确定性（熵），归一化后的熵作为权重混合LLM策略和PPO策略，以便在LLM高不确定时更多依赖自身策略，低不确定时信任LLM建议。

- **关键技术细节**：
  - **A*最优轨迹生成**：在MiniGrid网格世界中，基于曼哈顿距离启发式，用A*规划器计算从起点到任务目标的最优动作序列，产生结构化状态表示和符号动作，作为BERT的监督信号。
  - **BERT微调**：将环境状态（编码网格布局）转化为文本提示，使用bert-base-uncased，输入长度100 tokens，dropout率0.1，在21,500个A*生成样本上微调，训练为下一动作预测器。
  - **MC dropout不确定性估计**：在RL训练每个时间步，用微调后的BERT进行T=8次随机前向传递（dropout保持激活），得到T个动作概率向量，平均后得到P_LLM，同时计算其熵H。
  - **熵归一化**：将熵H归一化到[0,1]得到H_norm，公式为(H - H_min)/(H_max - H_min)。
  - **策略融合**：最终策略P_final = (1 - H_norm) * P_LLM + H_norm * P_agent，其中P_agent由当前PPO策略给出。这实现了在LLM不确定时（熵高）更多依赖自身策略，确定时（熵低）更多依靠LLM引导。
  - **PPO更新**：使用GAE（λ=0.95）、裁剪（ε=0.2），每50个episodes更新一次，存储经验元组。
  - **算法流程**（Algorithm 1）：每个episode初始化状态，循环直到终止：状态转文本→MC dropout获得P_LLM和H_norm→融合得P_final→采样动作→执行得到转移→存入buffer→buffer满时用PPO更新。

- **公式**：P_final = (1 - H_norm)·P_LLM + H_norm·P_agent（式1）

## 3. 实验设计：数据集/场景、基准、对比方法

- **环境与场景**：使用MiniGrid的UnlockPickup环境（v0版本），包含三个有序子任务：拿起钥匙、开锁门、到达目标。奖励稀疏（仅子任务完成时获得非零奖励）。评估在4×4和8×8两种网格大小上进行，每个episode最大步数50。
- **基准/对比方法**：
  - Unguided RL (PPO)：无额外引导的标准PPO。
  - Uncalibrated LLM：直接使用LLM建议但不进行不确定性校准。
  - Linear RL (PPO)：可能指线性基线？文中未详细说明，但与PPO类似。
  - Q-Learning (GRPO)：使用群体相对策略优化的Q学习。
  - DQN (GRPO)：基于稠密网络的深度Q学习。
  - 对比指标：成功率（Success Rate）、平均步数（Avg Steps to Goal）、平均奖励（Average Reward）、总奖励（Total Reward）、总步数（Total Steps）、奖励AUC（Reward AUC）。
- **自对比**：不同dropout率和前向传递次数（表3）的敏感性分析。

## 4. 资源与算力

- **显式声明**：论文中未明确说明GPU型号、数量、训练时长。仅在实现细节中提及：
  - 使用bert-base-uncased（约1.1亿参数）进行微调。
  - 训练1000个episodes，每episode最多50步。
  - 使用固定随机种子42。
  - 优化器：AdamW，lr=5e-5，batch size=16，epochs=5。
- **分析**：由于缺少硬件信息，无法准确评估算力需求。但基于模型规模和1000个episodes（每步8次前向传播），训练开销适中，可在单GPU上完成。

## 5. 实验数量与充分性

- **实验数量**：
  - 主对照实验（表2）：在4×4和8×8两种环境大小上，对比5种方法（ULPS、Uncalibrated LLM、Unguided RL、Q-Learning、DQN），共10组。
  - 校准指标对比（表1）：对比本文模型与Shoaeinaeini & Harrison（2024）在4×4上的精度、ECE、Brier Score、DA。
  - 超参数实验（表3）：9种dropout率×前传次数组合对平均步数和AUC的影响。
  - 消融分析：通过表2间接对比（PPO vs Uncalibrated LLM vs combined），无显式消融表的独立实验。
- **充分性评价**：
  - **优点**：对比方法覆盖了几种主流RL基线，包含未校准和未引导变体；进行了超参数敏感性分析；区分了4×4和8×8两种难度。
  - **不足**：
    1. 仅在一个特定环境（MiniGrid UnlockPickup）上验证，任务多样性和环境泛化性有限。
    2. 所有实验基于固定随机种子42，未提供多次运行的平均值和标准差，因此无法评估结果的统计稳定性和随机误差。
    3. 缺乏与其他稀疏奖励专用方法（如HER、ICM、RND）的对比。
    4. 消融不够系统（例如未单独移除MC dropout或熵混合机制定量分析）。
  - **公平性**：实验设置中统一了PPO超参数和训练步骤，但不同方法可能对超参数敏感，未进行单独调优；固定种子可能掩盖某些方法的方差。

## 6. 主要结论与发现

- ULPS在4×4环境中达到99.9%成功率、2055.08奖励AUC、平均仅7.24步；在8×8环境中达到99.7%成功率、1886.80 AUC、15.37步，显著优于所有对比方法。
- 未校准LLM虽好于纯RL，但在步数和成功率上明显逊于校准版本（4×4：94.0% vs 99.9%，18.39步 vs 7.24步）。
- 传统方法（Unguided RL、DQN）在稀疏奖励下几乎失败，成功率极低，步数极高。
- 基于MC dropout的不确定性校准是性能提升的关键：降低Brier Score（0.06 vs 0.20），提高区分能力（DA=1.0），并有效避免过度信任不可靠建议。
- 熵基混合机制成功实现了从LLM引导到策略自主的平滑过渡，减少了无效探索。

## 7. 优点

- **方法创新性**：首次将A*符号轨迹微调的LLM与MC dropout不确定性估计无缝集成到PPO中，提出熵归一化混合策略，兼具先验知识和自适应控制。
- **样本效率显著提升**：ULPS在4×4环境中仅需7,286步达到99.9%成功率，而Uncalibrated LLM需要20,284步，Unguided RL需要49,147步，效率提升86%以上。
- **无需人类标注**：A*自动化生成最优轨迹，微调完全自监督，可扩展至不同任务。
- **理论依据明确**：从不确定性校准角度处理LLM可靠性问题，逻辑清晰。
- **结果全面**：报告了多种指标（成功率、步数、奖励、AUC、校准指标），便于综合评估。

## 8. 不足与局限

- **环境多样性不足**：仅在MiniGrid UnlockPickup一个任务上测试，网格世界结构简单，状态可完全观测，距离真实机器人或部分可观测场景差距大。
- **缺乏统计可靠性**：未报告多次运行均值与方差，固定种子结果可能存在偏见，结论的稳健性存疑。
- **消融实验不充分**：未单独验证MC dropout、熵混合、A*微调各组件贡献量；对比方法中缺少直接移除某组件的消融。
- **计算开销分析不完整**：MC dropout带来8倍推理开销，论文仅定性讨论“整体训练效率高”，但未与更高效的不确定性估计方法（如集成、证据深度学习）对比。
- **泛化性未验证**：LLM仅在相同环境生成数据上微调，未测试到未见过的布局或任务结构变化时是否仍有效。
- **未与当前最先进稀疏奖励方法对比**：如HER、RND、ICM、LLM-RL混合的其他方案（如SayCan、Prompt重写）。
- **潜在偏差风险**：固定种子可能导致结果对种子敏感；仅使用一个环境可能偶然有利；LLM提示模板固定，未探索不同提示设计的影响。

（完）
