---
title: Query-Limited Community Recovery in Stochastic Block Models
title_zh: 随机块模型中的查询受限社区恢复
authors: "Sabyasachi Basu, Manuj Mukherjee, Lutz Oettershagen, Suhas Thejaswi"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02055v1"
tags: ["query:graph-part"]
score: 9.0
evidence: 随机块模型中的社区恢复（图划分）
tldr: 本文研究随机块模型中受限于带噪邻居查询的社区恢复问题。在仅访问oracle时，平衡均匀查询的非自适应阈值非最优，自适应两阶段策略在特定参数下只需n+o(n)次查询，而均匀查询需多倍。当额外获得子采样图时，自适应查询可针对少量不确定顶点，实现亚线性查询预算下的精确恢复，而均匀无改善。结果表明自适应数据获取能提升信息论恢复极限。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02055v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1715, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02055v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1403, \"height\": 606, \"label\": \"Table\"}]"
motivation: 探究有限带噪网络中通过自适应查询提高社区检测精度的信息论极限。
method: 设计两阶段自适应查询策略，先初始化估计社区，再针对不确定节点追加查询，并利用子采样图辅助。
result: 自适应策略在仅oracle时以n+o(n)查询超越均匀查询的多倍需求；有子采样图时，亚线性查询可达精确恢复。
conclusion: 自适应数据获取可严格改进精确恢复的信息论界限。
---

## 摘要
我们研究了在有限且带噪声的网络数据访问下，顶点数为n的两社区随机块模型中的精确社区恢复问题。学习者可以查询一个带噪声的邻域预言机，该预言机以固定概率独立返回被查询顶点的每个真实邻居，并且绝不返回非邻居，但受到有限查询预算的限制。我们考虑了仅预言机访问以及结合模型，其中学习者还观察到基础图的一个子采样副本。对于仅预言机访问，均匀平衡查询给出了一个尖锐的非自适应基准：当每个顶点被查询相同整数次数时，观测结果简化为具有衰减边概率的随机块模型，并适用Abbe-Bandeira-Hall精确恢复阈值。我们证明这个基准并非自适应最优：一个两阶段自适应策略在某种场景下成功使用n+o(n)次查询，而均匀平衡查询需要mn次查询，其中m>1。当有额外的子采样图时，我们证明了亚线性查询的自适应差距：预算亚线性时的平衡数据无关均匀查询不会比单独的子采样图有所改进，而自适应查询可以针对少量不确定顶点并实现精确恢复。因此，自适应数据采集可以严格改善精确恢复的信息论极限。

## Abstract
We study exact community recovery in the two-community stochastic block model on $n$ vertices under limited and noisy access to network data. The learner may query a noisy neighborhood oracle that reveals each true neighbor of a queried vertex independently with fixed probability and never returns non-neighbors, subject to a finite query budget. We consider both oracle-only access and a combined model where the learner also observes a single subsampled copy of the underlying graph. For oracle-only access, balanced uniform querying gives a sharp non-adaptive benchmark: when each vertex is queried the same integer number of times, the observations reduce to an SBM with attenuated edge probabilities and the Abbe-Bandeira-Hall exact-recovery threshold applies. We show that this benchmark is not adaptively optimal: a two-stage adaptive strategy succeeds with $n+o(n)$ queries in a regime where balanced uniform querying requires $m n$ queries for some $m>1$. With an additional subsampled graph, we prove a sublinear-query adaptivity gap: balanced data-independent uniform querying with a sublinear budget does not improve over the subsampled graph alone, whereas adaptive querying can target a small set of uncertain vertices and achieve exact recovery. Thus adaptive data acquisition can strictly improve the information-theoretic limits of exact recovery.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 本文研究**随机块模型（SBM）** 中**精确社区恢复**问题，考虑两种信息获取受限的场景：
  - **仅预言机访问**：学习者只能通过有限次查询一个带噪声的邻域预言机来获取局部邻居信息（每次查询以概率 w 返回每个真实邻居，绝不返回非邻居）。
  - **子采样图 + 预言机访问**：学习者先观察到一个边以概率 s 保留的子采样图 Gsub，然后再使用预言机查询。
- 核心动机：在有限且带噪声的观测资源下，**自适应数据采集**（即根据已观测信息决定后续查询哪些顶点）能否比**均匀非自适应查询**（均匀分配查询次数）带来更优的精确恢复信息论极限？
- 意义：将数据获取本身纳入推理问题，揭示了**自适应查询可以严格改善精确恢复阈值**，拓展了传统被动观测模型（如相关 SBM）的研究。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- 采用**两阶段自适应策略**：第一阶段使用部分预算进行均匀或粗粒度查询，获得一个粗略的全局社区标号，并识别出一小部分标号仍不确定的顶点；第二阶段将剩余预算集中用于这些不确定顶点，利用**留一法（leave-one-out）** 构造的参考标号和新鲜预言机查询进行局部似然比测试，修复错误。

### 关键技术细节
- **留一法筛选框架**：
  - 对每个顶点 v，计算一个**留一参考标号** $\hat{\sigma}^{(-v)}$（排除 v 的观测），并用一个独立的比较集 Cv 决定 v 是否不确定（筛选），用另一个独立的参考集 Bv 用于第二阶段似然比测试。
  - 这种分离保证了筛选信息不会污染后续修复所使用的证据。
- **均匀非自适应基准**（Proposition 1）：
  - 若每个顶点查询次数 m = k/n 为整数，则观测图等价于边概率衰减为 $1-(1-w)^{2m}$ 的 SBM，其精确恢复阈值为 $\theta_{\text{unif}}(k,w)(\sqrt{\alpha}-\sqrt{\beta})^2 > 2$，其中 $\theta_{\text{unif}}=1-(1-w)^{2k/n}$。
- **自适应策略**（Algorithm 1）：
  - 阶段一：均匀查询 x₀n 次，获得初步标号 $\hat{\sigma}^{(0)}$ 和留一参考标号，定义筛选集 A = {v | 局部边际太小}。
  - 阶段二：对每个 v∈A 额外查询 L 次，基于新鲜观测和参考集 Bv 进行似然比检验，判定 v 的社区。
  - 成功条件：存在 γ<1, ζ∈(0,1] 满足 $\frac{\zeta}{2}(1-(1-w)^L)\Delta^2 > \gamma$，使得 |A| ≤ n^γ，且参考集误差可忽略（Lemma 4, Theorem 3）。

### 示例参数
- Example 1：取 α=16, β=1, w=1-2^{-1/6}，阶段一每顶点查询 1 次（k₀=n），筛选得到 γ≈0.88，ζ=1/2，L=5 满足条件，总查询 n+o(n) 实现精确恢复，而均匀非自适应需要约 1.087n 次查询。

## 3. 实验设计
- **本文为纯理论分析论文，未使用真实数据集或合成数据实验**。
- 所有结果基于**数学证明**和**渐近分析**（概率为 1-o(1) 的断言）。
- 论文通过**构造性示例**（如 Example 1）和**反例论证**（如 Theorem 5 证明线性查询必要性，Theorem 8 证明自适应差距）来验证理论。
- 基准方法：均匀非自适应查询（基于经典 ABH 阈值），以及仅子采样图（无预言机）的情况。

## 4. 资源与算力
- **本文未涉及任何计算实验**，因此无 GPU、训练时长等资源描述。
- 所有结论均源自理论推导，不依赖数值模拟。

## 5. 实验数量与充分性
- 由于是理论论文，不存在传统意义上的“实验数量”。但论文提供了：
  - 定理 3、5、8 的严格证明。
  - 引理 4（具体参数下的筛选）和引理 6、7 的详细构造。
  - 多个例子（Example 1）和参数选择展示自适应优势。
- 充分性：覆盖了仅预言机模型（线性查询阈值）和子采样图+预言机模型（亚线性查询阈值），并证明了自适应差距的存在，论证严谨。但作者也指出**未刻画最优常数**和**完整自适应相图**，留作未来工作。

## 6. 论文的主要结论与发现
1. **仅预言机模型**：
   - 均匀非自适应查询可达到尖锐的 ABH 阈值。
   - 自适应两阶段策略能在更小的线性预算（n+o(n) vs. mn, m>1）下实现精确恢复，证明常数级别的自适应差距。
   - 任何成功策略至少需要 Ω(n) 次查询（Theorem 5），故自适应不能降阶。
2. **子采样图+预言机模型**：
   - 亚线性预算（k=o(n)）下，均匀非自适应查询无法提升子采样图单独的阈值。
   - 自适应查询可利用 Gsub 识别少量不确定顶点，以亚线性预算实现精确恢复，产生**真正的亚线性自适应差距**（Theorem 8）。
3. 核心启示：**自适应数据获取可以严格改善精确恢复的信息论极限**，且存在侧信息时收益更显著。

## 7. 优点
- **问题新颖**：将数据获取决策纳入社区恢复问题，研究自适应查询的信息论极限。
- **方法精巧**：留一法筛选框架既解决了自适应选择造成的依赖问题，又提供了可分析的参考集；两阶段策略避免了混合不同来源信息的偏差。
- **结论清晰**：用具体参数示例展示了自适应优势，并用量化下界（Ω(n) 查询）证明结论的强度。
- **技术贡献**：拓展了 SBM 领域的研究，从被动观测到主动数据获取。

## 8. 不足与局限
- **渐近性**：所有结果为 n→∞ 情形，未讨论有限 n 的实际性能。
- **参数局限**：仅分析了两社区、平衡、对数度 SBM，未推广到多社区、不平衡或更一般的度分布。
- **缺失最优常数**：自适应差距的具体数值（如 m 的最优值）未确定，仅以存在性展示。
- **未涉及计算复杂度**：论文专注信息论可行性，未给出高效算法（MLE 可能计算困难）。
- **未考虑噪声模型扩展**：预言机假设只有单侧噪声（不返回非邻居），更实际的噪声模型（如误报）未讨论。
- **无实验验证**：缺乏仿真实验来验证理论预测的有限样本行为，但理论论文本身不要求实验。

（完）
