---
title: Bellman-Taylor Score Decoding for Markov Decision Processes with State-Dependent Feasible Action Sets
title_zh: 面向状态相关可行动作集的马尔可夫决策过程的贝尔曼-泰勒得分解码
authors: "Yi Chen, Rushuai Yang, Qiang Chen, Dongyan, Huo"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10979v1"
tags: ["query:graph-part"]
score: 8.0
evidence: 提出一种新的深度强化学习框架处理状态相关动作集合的MDP
tldr: 运筹学中的马尔可夫决策过程（MDP）常具有状态依赖的可行动作集，由隐式操作约束定义，导致标准深度强化学习（DRL）算法难以应用。本文提出Bellman-Taylor score decoding框架，基于最优动作值函数的泰勒展开，将策略学习映射到欧几里得分数空间，并通过动作解码器强制可行性，避免解码器求导。该方法提供理论性能保证，最优性间隙分解为结构逼近误差和算法学习误差。在排队网络控制问题中，策略学习出状态依赖的索引调度规则，小实例近乎最优，大系统显著优于基准方法，展示了框架的有效性和可扩展性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10979v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1664, \"height\": 383, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1702, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1281, \"height\": 351, \"label\": \"Table\"}]"
motivation: 现有DRL无法处理状态依赖的隐式约束动作空间，需要新方法在保持可行性的同时兼容标准优化算法。
method: 受动作值函数泰勒展开启发，构造分数空间转移策略学习，设计动作解码器从分数映射到可行动作集。
result: 理论推导出最优性间隙的分解形式，排队控制实验中小实例接近最优，大系统性能显著优于基准。
conclusion: 所提框架有效解决状态依赖约束MDP的DRL问题，兼具理论保证与实用扩展性。
---

## 摘要
运筹学中的许多马尔可夫决策过程（MDP）具有状态相关的可行动作，这些动作由各种运营约束隐式定义。这些特性使得难以使用标准的深度强化学习（DRL）算法，因为其动作接口通常假定固定的有限动作目录或简单的欧几里得空间。受最优动作价值函数的泰勒展开启发，我们提出了贝尔曼-泰勒得分解码框架，该框架将策略学习转移到欧几里得得分空间，同时通过动作解码器确保可行性。由此产生的潜在得分MDP可以通过标准DRL算法进行优化，而无需对解码器进行微分。我们提供了性能保证，表明该方法的优化差距分解为结构近似误差和算法学习误差。最后，我们将该框架应用于排队网络控制问题，其中策略本质上学习了状态相关的基于指标的调度规则。数值实验显示，在小型实例中接近最优性能，并在较大系统中相比基准有显著改进。

## Abstract
Many Markov decision processes (MDPs) in operations research have feasible actions that are state dependent and defined implicitly by various operational constraints. These features make it difficult to use standard deep reinforcement learning (DRL) algorithms, whose action interfaces typically assume either a fixed finite action catalog or a simple Euclidean space. Motivated by a Taylor expansion of the optimal action-value function, we propose Bellman--Taylor score decoding, a framework that moves policy learning to a Euclidean score space while enforcing feasibility through an action decoder. The induced latent-score MDP then can be optimized by standard DRL algorithms without differentiating through the decoder. We provide a performance guarantee showing that the optimality gap of this approach decomposes into a structural approximation error and an algorithmic learning error. Lastly, we apply this framework to a queueing network control problem, where the policy essentially learns a state-dependent index-based dispatching rule. Numerical experiments show near-optimal performance in small instances and considerable improvements over benchmarks in larger systems.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，生成一份结构化、深入、客观的中文总结。

---

### 贝尔曼-泰勒得分解码：面向状态相关可行动作集的马尔可夫决策过程

#### 1. 核心问题与研究动机

*   **核心问题**：传统的深度强化学习（DRL）算法在处理运筹学（OR）中的马尔可夫决策过程（MDP）时遇到瓶颈。许多OR问题的动作空间是**状态依赖的**、**隐式定义的**，并受**组合约束**（如容量、兼容性、整数约束）影响，这与标准DRL算法假设的固定有限动作目录或简单欧几里得空间不兼容。
*   **研究动机**：现有的解决方案（如动作掩码、动作嵌入、优化层）要么依赖于可枚举的动作集，要么需要问题特定的定制化算法，缺乏通用性和可迁移性。因此，需要一种新框架，能够将**标准DRL算法**直接应用于这类复杂MDP，同时**严格保证可行性**。

#### 2. 方法论：贝尔曼-泰勒得分解码

*   **核心思想**：将策略学习与可行性验证分离。框架通过一个**动作解码器**将非标准、约束性的原始动作空间转换到一个标准的、无约束的**欧几里得得分空间**。DRL算法只需在这个简单的得分空间上学习策略，而可行性则由解码器通过求解一个优化问题来保障。
*   **关键技术细节**：
    1.  **结构假设**：假设MDP具有“动作后配置”结构，即状态转移可表示为 `s' = Ξ_s(ϕ_s(a), ξ_s)`。其中 `ϕ_s(a)` 是动作后配置，`ξ_s` 是随机扰动。
    2.  **泰勒展开近似**：对最优动作价值函数 `Q*(s, a)` 中的“任务后延续价值函数” `G*_s(ϕ_s(a))` 进行一阶泰勒展开，得到近似最优动作选择标准：`argmax_{a∈A(s)} {ψ_s(a) + ⟨z*_1(s), ϕ_s(a)⟩}`。这个线性形式定义了动作解码器。
    3.  **动作解码器（Γ）**：给定一个状态 `s` 和一个潜在得分向量 `z`，解码器通过求解一个优化问题来选择可行动作：`Γ(s, z) ∈ argmax_{a∈A(s)} {ψ_s(a) + ⟨z, ϕ_s(a)⟩}`。这确保了所选动作严格可行。
    4.  **潜在得分MDP**：原始MDP被转换为一个新的“潜在得分MDP”，其状态空间不变，但动作空间变为欧几里得空间 `R^{d_s}`。标准连续动作DRL算法（如PPO）可以直接应用于此MDP。
    5.  **无需反向传播**：策略梯度更新 `∇θ log π_θ(z|s)` 不依赖于动作解码器的梯度，因此可以跳过对解码器（可能不连续）的微分，这是与可微分优化层方法的关键区别。
    6.  **高阶推广**：对于非线性较强的系统，可以将泰勒展开扩展到高阶，使用更丰富的特征（如二次项 `ϕ_s(a)^2`）来构建解码器 `Γ_K`，以更好地近似最优动作。
*   **理论保证（定理4.2）**：论文提供了性能保证，将最优性差距分解为：
    -   **结构近似误差**：由泰勒展开的残差振荡 `Ω_s(z)` 决定，衡量了线性得分无法解释的延续价值变化。
    -   **算法学习误差**：由PPO等算法在求解潜在得分MDP时引入。

#### 3. 实验设计

*   **场景与数据集**：
    1.  **库存控制问题**：一个多地点、可跨地转运的库存控制问题，用于“概念验证”（sanity check）。
    2.  **排队网络控制问题**：一个更复杂的、多类别、多服务池的调度问题，是本文的主要案例研究。包括小规模（2x2网络）和中规模（5x5网络）设置。
*   **Benchmark与对比方法**：
    -   在排队网络中，对比了四种经典的基于索引的调度启发式规则：`cμ` 规则、修正 `cμ` 规则、最大权重规则 (max-weight)、修正最大权重规则。
    -   对比了其他DRL方法：原子PPO（Atom-PPO，一种将组合动作分解为序列动作的方法）和基于混合整数规划（MIP）的值前瞻算法。
    -   进行了消融实验：将贝尔曼-泰勒得分解码（BTSD）与不同的DRL骨干网络（PPO、SAC、DQN）结合，并与它们的原始版本（未使用BTSD）进行比较。
*   **评估指标**：在小型问题中，与通过精确动态规划计算的最优解进行比较，使用**最优性差距**、**最优动作一致率**和**贝尔曼后悔值**。在中型问题中，报告平均成本及其标准误差。

#### 4. 资源与算力

*   **未明确说明**：论文文本中**没有明确提及**使用了具体的GPU型号、数量或训练时长等计算资源信息。

#### 5. 实验数量与充分性

*   **实验数量**：
    -   库存控制实验：4个不同非线性参数（ρ）的实例。
    -   排队网络小规模实验：2种负载配置 × 3种溢出成本水平 = 6个实例。
    -   排队网络中规模实验：3种流量负载制度 × 3种成本结构实例 = 9个实例。
    -   消融实验：对比了3种DRL骨干网络，共6组实验（3个原始 + 3个BTSD版本）。
*   **充分性与客观性**：实验设计较为充分。涵盖了从简单验证到复杂系统的多个场景，对比了经典的OR启发式、其他前沿DRL方法，并进行了消融研究。实验结果清晰地展示了BTSD框架在所有测试案例中的优势，对比方法的选取具有代表性。实验设计是客观和公平的。

#### 6. 主要发现与结论

1.  **有效性**：所提出的贝尔曼-泰勒得分解码（BTSD）框架能有效解决状态依赖、高度约束的DRL问题。
2.  **接近最优**：在可以计算最优解的小型排队网络实例中，BTSD-PPO的性能差距在2.5%以内。
3.  **显著超越基准**：在更大、更现实的排队网络控制问题中，BTSD-PPO在所有9个实例上均优于所有基准方法，改进幅度从4.0%到23.7%。
4.  **普适性与解耦性**：消融实验证明，BTSD框架带来的性能提升是与底层DRL优化器无关的，它可以显著改善PPO、SAC、DQN等不同算法的性能。这表明收益主要来自BTSD的动作接口，而非特定优化器。
5.  **学得规则**：在排队控制问题中，BTSD框架学出了一种状态依赖的“索引调度规则”，可以理解为一种数据驱动的、优于传统启发式的`cμ`规则。

#### 7. 优点与亮点

1.  **方法创新性高**：巧妙地将泰勒展开与DRL结合，提供了一个全新的、理论驱动的视角来处理复杂动作空间问题。
2.  **通用性与模块化**：框架将“学习”与“可行性”解耦，允许使用任何标准连续动作DRL算法作为学习器，而将领域知识封装在解码器中，增强了其作为通用工具的潜力。
3.  **理论保障**：提供了清晰的最优性差距分解，解释了方法的误差来源，并为算法的适用性提供了理论依据。
4.  **无需微分**：动作解码器不参与反向传播，避免了处理组合优化问题中不连续梯度的问题，使得框架可以被容易地插入现有DRL管线。
5.  **实验验证扎实**：实验设计覆盖了从概念验证到复杂案例的多个层面，对比了多种强基准，并提供了消融分析，结论说服力强。

#### 8. 不足与局限

1.  **对模型结构的要求**：框架假设MDP具有“动作后配置”结构，这限制了其应用范围。对于无法提供此类自解释的、结构化状态表示的“黑箱”问题，该方法不直接适用。
2.  **计算开销**：每次动作选择都需要解决一个优化问题（即解码器），这可能带来显著的计算开销，尤其是在动作空间巨大或约束非常复杂时。
3.  **实验未覆盖的领域**：未在更广泛的、非排队类别的运筹学问题（如大规模资源分配、路径规划）上进行验证。也没有讨论离线学习或平均成本标准下的应用。
4.  **资源开销未报**：论文没有报告计算资源消耗（GPU、时间），这使得评估其实际部署成本变得困难。
5.  **对泰勒近似的依赖**：方法的核心是基于一阶（或高阶）泰勒展开的局部近似，当最优价值函数存在巨大、尖锐的曲率或不可微点时，结构近似误差可能会变大，影响性能。

（完）
