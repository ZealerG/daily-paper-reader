---
title: Baseline-Free Policy Optimization for Neural Combinatorial Optimization
title_zh: 无基线的神经组合优化策略优化
authors: "Carlos S. Sepúlveda, Gonzalo A. Ruz"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10321v1"
tags: ["query:graph-part"]
score: 9.0
evidence: 用于组合优化的无基线策略优化方法
tldr: "神经组合优化中，标准REINFORCE依赖rollout baseline，在难实例上产生噪声梯度导致训练崩溃（如TSP-100成本从9.8骤升至52.1且不恢复）。本文引入来自大语言模型对齐的GRPO，通过组内轨迹优势归一化完全消除外部baseline。在TSP和CVRP上，GRPO避免训练崩塌，且以匹配梯度更新达到与强基线POMO相差2%以内的求解质量。GRPO为NCO提供了一种无需外部baseline的鲁棒且简洁的训练方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10321v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10321v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1622, \"height\": 605, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10321v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 986, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10321v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1014, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10321v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1216, \"height\": 322, \"label\": \"Table\"}]"
motivation: 标准REINFORCE依赖rollout baseline，难实例上噪声梯度导致训练不稳定甚至崩溃。
method: 引入GRPO，通过组内轨迹优势归一化消除外部baseline，实现无基线策略优化。
result: "GRPO避免TSP-100训练崩溃，性能接近POMO（差距<2%），且P3O变异性高。"
conclusion: GRPO是NCO中无需外部baseline的鲁棒替代，适用于baseline易失效场景。
---

## 摘要
神经组合优化（NCO）训练自回归策略以解决路由问题。标准训练算法——使用rollout基线的REINFORCE——需要维护并定期更新策略的冻结副本以进行方差缩减。这种基线引入了一个结构性弱点：在较难的实例上，差的基线会产生噪声梯度估计，可能破坏训练稳定性。我们评估了组相对策略优化（GRPO），这是一种来自大语言模型对齐的算法，它通过归一化采样轨迹组内的优势完全消除了基线。在RL4CO框架内对TSP和CVRP基准上的五种强化学习算法进行控制比较后，我们发现：（i）GRPO避免了REINFORCE在TSP-100上观察到的训练崩溃，后者的性能在热身阶段后立即从成本9.8退化到52.1，且在延长训练下无法恢复；（ii）在匹配的梯度更新次数下，GRPO的解决方案质量与POMO（一种基于AM的强多起点基线）相差在2%以内，同时无需外部基线；（iii）同样来自对齐文献的成对偏好算法P3O在TSP上具有竞争力，但在CVRP上表现出更高的变异性。这些结果表明GRPO是NCO中一种有前景的无基线替代方案，特别是在基线依赖训练变得脆弱的场景中。

## Abstract
Neural combinatorial optimization (NCO) trains autoregressive policies to solve routing problems. The standard training algorithm, REINFORCE with a rollout baseline, requires maintaining and periodically updating a frozen copy of the policy for variance reduction. This baseline introduces a structural vulnerability: on harder instances, a poor baseline produces noisy gradient estimates that can destabilize training. We evaluate Group Relative Policy Optimization (GRPO), an algorithm from large language model alignment that eliminates the baseline entirely by normalizing advantages within groups of sampled trajectories. In a controlled comparison of five RL algorithms on TSP and CVRP benchmarks within the RL4CO framework, we find that: (i) GRPO avoids the training collapse observed with REINFORCE on TSP-100, where performance degrades from cost 9.8 to 52.1 immediately after the warmup phase and does not recover under extended training; (ii) at matched gradient updates, GRPO achieves solution quality within 2% of POMO, a strong AM-based multi-start baseline, while requiring no external baseline; and (iii) P3O, a pairwise preference algorithm also from the alignment literature, is competitive on TSP but shows higher variability on CVRP. These results identify GRPO as a promising baseline-free alternative for NCO, particularly in settings where baseline-dependent training becomes fragile.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容和要求，生成的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：神经组合优化（NCO）中，主流的训练算法（如REINFORCE）依赖于一个外部“基线（baseline）”来进行方差缩减（通常是策略的冻结副本生成的贪心解成本）。这种基线依赖存在一个**结构性风险**：在较难的实例上，策略和基线相互依赖，产生噪声梯度，可能导致训练不稳定甚至完全崩溃。
*   **研究动机**：本文旨在探索一种**无需外部基线**的策略优化算法，以解决标准方法在复杂问题上的脆弱性问题，并为NCO领域提供更稳健的训练方案。
*   **整体含义**：论文验证了起源于大语言模型（LLM）对齐领域的**组相对策略优化（GRPO）** 算法在NCO任务中的有效性。GRPO通过组内轨迹的归一化优势计算，完全摆脱了对外部基线的依赖，避免了训练崩溃，并在计算资源公平比较下，达到了与强基线方法（如POMO）接近的性能。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：引入 **GRPO**，其核心是 **“自包含”的优势计算**。对于每个实例，算法生成一组（G个）轨迹，并基于这组轨迹内部的相对表现（而非与某个外部参考比较）来计算每条轨迹的优势值。
*   **关键技术细节**：
    *   **优势计算（公式化）**：对于每个批次中的一个实例，生成G条轨迹 \(\tau_g\)。其优势 \(\hat{A}_g\) 的计算方式为：
        \[
        \hat{A}_g = \frac{R(\tau_g) - \mu_G}{\max(\sigma_G, \epsilon)}
        \]
        其中 \(R(\tau)\) 是轨迹的奖励（-C(τ)），\(\mu_G\) 和 \(\sigma_G\) 是该组G个轨迹奖励的均值和标准差，\( \epsilon \) 是一个小常数用于防止除零。这本质上是对组内优势进行 **z-score归一化**。
    *   **策略更新**：使用类似PPO的裁剪目标函数更新策略，确保更新步长稳定。
    *   **温度退火（Temperature Annealing）**：为了确保组内轨迹的多样性，防止在训练初期因采样温度过低导致所有轨迹相同（\(\sigma_G\)接近0），算法采用了从初始温度\(T_{init}=2.0\)到最终温度\(T_{final}=1.0\)的线性退火策略，以保证早期能进行充分探索。

*   **算法流程**：
    1.  **采样**：对于批次中的每个实例，使用当前策略以温度T采样G条轨迹。
    2.  **组内归一化**：计算G条轨迹的奖励均值\(\mu_G\)和标准差\(\sigma_G\)，进而得到每个轨迹的归一化优势\(\hat{A}_g\)。
    3.  **内循环更新**：利用收集到的所有BG（B为批次大小，G为组大小）条轨迹数据，使用PPO裁剪目标进行**K次内循环**更新（K=2，小批量更新）。

### 3. 实验设计

*   **数据集/场景**：四个标准NCO基准：**TSP-50, TSP-100, CVRP-50, CVRP-100**。节点坐标在[0,1]²上均匀采样。
*   **Benchmark框架**：使用统一的**RL4CO**框架，所有算法共享相同的**Attention Model (AM)** 架构（6层Transformer编码器，8个注意力头，128维嵌入）。
*   **对比方法**：
    *   **GRPO**（本文提出的方法）
    *   **P3O**（来自LLM对齐，成对偏好优化）
    *   **POMO**（AM的强基线，使用多起点和共享基线）
    *   **REINFORCE**（标准方法，使用rollout基线）
    *   **PPO**（RL4CO中的实现，使用episode级评论家网络）

### 4. 资源与算力

*   所有实验均在**单个** **NVIDIA RTX PRO 6000 Blackwell Max-Q GPU** 上完成。
*   论文未提供每个实验或总体的具体训练时长。

### 5. 实验数量与充分性

*   **实验数量**：
    *   主实验：在4个数据集上对比了5种方法，每组2个随机种子。
    *   扩展实验：在最大实例（TSP-100, CVRP-100）上进行了**梯度更新次数匹配**的实验（POMO和REINFORCE训练800轮次，GRPO训练12轮次），以及REINFORCE的**超长训练（800轮次）**实验。
    *   总实验涵盖了大量独立训练的检查点（文中提到40个检查点），并进行了独立验证。
*   **实验充分性与公平性**：
    *   **充分性**：实验设置非常充分。不仅对比了主流的REINFORCE和POMO，还引入了来自其他领域的P3O和PPO，覆盖了主要竞争方法。**增梯度更新匹配实验**是核心亮点，它揭示了单纯按轮次对比可能导致的误导性结论，极大地增强了实验的公平性和说服力。
    *   **客观性**：论文明确指出了不同算法计算成本的差异（GRPO、P3O、PPO每轮做8倍更新），并主动设计了实验来解耦“轮次”和“梯度更新数”的影响，展现了高度的客观性。
    *   **控制变量**：所有方法共享相同的架构、基线框架（RL4CO）和大部分超参数，使得性能差异主要归因于算法本身。

### 6. 论文的主要结论与发现

1.  **GRPO避免了REINFORCE的训练崩溃**：在TSP-100上，REINFORCE在基线机制切换后立即崩溃（成本从9.8跳升至52.1），且无法恢复。GRPO则稳定收敛。
2.  **GRPO在匹配梯度更新次数下具有竞争力**：在公平比较下（相同梯度更新次数），GRPO的性能与POMO相差在 **1.5% - 1.6%** 之内。POMO在梯度利用效率上最优，GRPO则未能超越POMO。
3.  **REINFORCE的失败是结构性而非计算量不足**：即便将REINFORCE训练时间延长8倍（800轮次），其在TSP-100上的性能仍停留在第0轮水平，证明了其崩溃的根本原因是算法结构问题。
4.  **GRPO和POMO的效率权衡**：
    *   **POMO**：梯度更新效率高，每步更新提升显著。
    *   **GRPO**：数据利用效率高，能从少量实例中提取更多学习信号（通过组内多轨迹和内循环更新），在实例生成成本高的场景下更具优势。
5.  **P3O的表现**：在TSP上表现第二，但在CVRP上稳定性差（高变异性）。

### 7. 优点

*   **问题导向明确**：精准识别并实验验证了NCO领域主流方法存在的一个具体且严重的“基线脆弱性”问题。
*   **方法创新且简洁**：将LLM领域的GRPO引入NCO，其“自包含”优势计算思想优雅地解决了问题，无需维护外部基线，算法简洁。
*   **实验设计非常出色**：
    *   **公平性**：通过“梯度更新匹配”实验，清晰地揭示了算法性能差异的真正来源，这是本领域研究中值得效仿的榜样。
    *   **说服力**：对REINFORCE的崩溃现象进行了深入分析（检查点时机、延长训练），证明了问题的根本性，而非偶然。
*   **结论客观**：没有过分夸大GRPO的优势，明确指出其在相同计算资源下“没有超越”POMO，而是作为“互补方案”，体现了学术严谨性。

### 8. 不足与局限

*   **架构与问题局限性**：研究仅基于一种架构（6层AM）和两种问题（TSP, CVRP）进行。结论不必然推广到其他更强的图神经网络或更复杂的路由问题变体。
*   **搜索方法影响**：所有模型在推理时均使用**贪心解码**。作者指出，如果使用束搜索或采样等更复杂的搜索方法，结果排名可能发生变化。
*   **温度退火的潜在混淆**：GRPO和P3O使用了从2.0到1.0的温度退火策略，而REINFORCE和POMO使用恒定温度1.0。这种不对称性可能是一个潜在的混淆因素，尽管作者主张REINFORCE的崩溃与此无关。
*   **实践复杂度**：GRPO需要调整组大小G、内循环轮次K、温度退火计划等多个超参数，这为实际应用增加了调优成本。
*   **未覆盖场景**：未评估更大规模问题（如TSP-200+）或需结合课程学习的场景，而GRPO的稳定性优势可能在这些场景中更为显著。

（完）
