---
title: Fairness-Aware Profit Maximization using Deep Reinforcement Learning
title_zh: 基于深度强化学习的公平感知利润最大化
authors: "Poonam Sharma, Sanchit Virdi, Suman Banerjee"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29770v1"
tags: ["query:graph-part"]
score: 9.0
evidence: 深度强化学习应用于图划分以最大化利润
tldr: 社交网络中，考虑用户收益和成本，在预算约束下选择种子集以最大化利润，同时确保各社区获得公平的收益份额。针对该公平感知利润最大化问题，提出深度强化学习方法，将问题建模为马尔可夫决策过程并采用深度Q学习算法。在真实网络数据集上实验，该方法产生的种子集初始激活利润相比基线方法提升高达10倍。本工作为公平性约束下的网络影响力最大化问题提供了有效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29770v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29770v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29770v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1260, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29770v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1267, \"height\": 464, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29770v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1004, \"height\": 250, \"label\": \"Table\"}]"
motivation: 现有影响力最大化研究较少同时考虑利润和公平性，本文旨在预算约束下最大化利润并保障各社区收益公平。
method: 将问题建模为马尔可夫决策过程，采用深度Q学习算法学习最优种子选择策略。
result: 在真实社交网络数据集上，所提方法利润比基线方法最高提升10倍。
conclusion: 提出公平感知的利润最大化问题，并验证了深度强化学习方法的有效性。
---

## 摘要
给定一个以图表示的社会网络，其中节点是用户，边代表社会关系，以及一个正整数k，如何选择k个节点以最大化网络中的影响力仍是一个活跃的研究领域。在本文中，我们考虑该问题的一个变体，其中网络用户与两个参数相关联：收益值和成本。给定固定预算，并将网络划分为社区。任务是选择预算内的一个用户子集（种子集），使得它们的初始激活最大化所获利润，同时确保每个社区在最大最小公平准则下至少实现其总收益的最小比例。对于任意种子集，所获收益定义为受种子集影响的用户的收益值之和，利润定义为所获收益与总成本之差。正式地，我们称此为公平感知利润最大化问题。我们提出了一种基于深度强化学习的方法来解决它：首先将该问题建模为马尔可夫决策过程，随后提出一种深度Q学习算法。所提出的解决方案已在真实世界的社会网络数据集上实现并测试。从报告的结果中，我们观察到所提出的方法产生的种子集，其初始激活产生的利润比基线方法高达10倍。我们的方法实现可在https://github.com/PoonamSharma-PY/DRL_FPM.git获取。

## Abstract
Given a social network represented as a graph where the nodes are the users and the edges represent the social relations, and a positive integer k, how to select k nodes to maximize the influence in the network remains an active area of research. In this paper, we consider a variant of the problem in which network users are associated with two parameters: a benefit value and a cost. A fixed budget is given, and the network is partitioned into communities. The task is to select a subset of users (the seed set) within the budget so that their initial activation maximizes the earned profit, while ensuring that each community realizes at least a minimum fraction of its total benefit under a maximin fairness criterion. For any seed set, the earned benefit is defined as the sum of the benefit values of the users influenced by the seed set, and the profit is defined as the difference between the earned benefit and the total cost. Formally, we call this the Fairness-Aware Profit Maximization Problem. We propose a Deep Reinforcement Learning-based approach for solving it: we first model the problem as a Markov Decision Process and subsequently propose a Deep Q-Learning Algorithm. The proposed solution has been implemented and tested on real-world social network datasets. From the reported results, we observed that the proposed approach yields a seed set whose initial activation produces up to 10 times more profit than the baseline methods. The implementation of our methodology is available at https://github.com/PoonamSharma-PY/DRL_FPM.git.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：传统影响力最大化（Influence Maximization, IM）只关注最大化受影响的用户数量，而现实场景中用户有成本和收益，营销活动的目标是最大化利润。同时，社交网络通常划分为多个社区，若只追求总利润，可能导致利润集中在少数社区，其他社区获益极少，造成不公平。为此，本文引入**公平感知利润最大化**（Fairness-Aware Profit Maximization, FPM）问题，要求在预算约束下选择种子集，使利润最大化的同时，每个社区至少获得其总收益的一个最小比例（基于最大化最小公平准则）。
- **核心问题**：给定图 \(G(V,E)\)，每个节点有成本 \(c(v)\) 和收益 \(b(v)\)，预算 \(B\)，社区划分 \(C\)，阈值 \(\tau\)。选择种子集 \(S \subseteq V\) 使得总成本 \(\sum_{v\in S}c(v) \le B\)，且每个社区的收益实现比例 \(\eta_{C_i}(G,S) \ge \tau\)，目标是最大化利润 \(\Pi(S) = \text{受影响的收益总和} - \text{种子成本}\)。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将种子选择建模为马尔可夫决策过程（MDP），用深度Q学习（Deep Q-Learning）学习一个策略，能够跨不同图实例泛化，选择出满足预算和公平约束的种子集。
- **关键技术细节**：
  - **MDP建模**：
    - 状态 \(S_t = (G, C, X_t)\)，其中 \(X_t\) 为二进制选择向量（已选种子）。实际输入Q-network使用Structure2Vec嵌入节点特征（成本、收益）和邻居聚合，外加候选节点的成本、剩余预算、当前种子集大小（共67维）。
    - 动作：每时间步选择一个未选且预算可行的节点。
    - 奖励：每步的边际奖励 = 边际利润 + \(\phi \times\) 边际公平项（最大最小公平性）。公平项定义为最差社区的收益实现比例，权重 \(\phi=1.0\)。最终奖励为总利润+\(\phi\times\)公平值。
  - **网络嵌入**：使用Structure2Vec（DE-MF变体）产生节点嵌入 \(h_v\)，作为Q-network输入。
  - **深度Q学习算法RL4FPM**：
    - 训练循环：每 episode 从训练集中采样一个子图，用Structure2Vec生成嵌入；通过\(\epsilon\)-greedy策略选择节点（\(\epsilon\)从1.0乘性衰减至0.05）；每步存储经验到回放缓冲区（容量10,000）；每2 episodes用32的mini-batch进行梯度更新（Adam，lr=0.001，折扣因子0.95）。
    - 算法流程：伪代码见Algorithm 1。
  - **时间复杂度**：训练时每episode的复杂度 \(O(H \cdot |V| \cdot |\theta| + |E| + T_{\text{emb}}|E|d_{\text{emb}})\)；推理时 \(O(T_{\text{emb}}|E|d_{\text{emb}} + H|V||\theta|)\)，比传统贪心方法（需 \(O(H\cdot |V|\cdot m\cdot |E|)\)）更高效。

### 3. 实验设计：数据集、场景、基准方法

- **数据集**：
  - 真实网络：Email-Eu-Core（1,005节点，25,571边）、H-BA2k（30,000节点，119,990边）、Wiki-Vote（7,115节点，103,689边）。
  - 所有数据集按80:20比例划分为多数和少数社区。训练/测试子图实例数见表1（Email: 12训练/8测试；H-BA2k: 50/10；Wiki-Vote: 12/8）。
- **场景**：
  - 两种影响概率设置：Uniform（所有边概率0.1）和 Trivalency（每条边从{0.1, 0.01, 0.001}随机选取）。
  - 预算值：500、1000、1500、2000、2500、3000。
- **基准方法**：Random（随机选种子）、Simple-PageRank（按PageRank选top-k）、Parity（按度公平选种）、High Degree（按度选top-k）、Fair-PageRank（Parity+PageRank）、Crosswalk（公平感知嵌入+k-medoids选种子）。
- **评估指标**：基于1,000次蒙特卡洛模拟的利润平均值。

### 4. 资源与算力

- **文中说明**：实验在一台Linux工作站上进行，配备32核Intel处理器(3.2GHz)和64 GiB内存。所有算法使用Python 3.13.5实现，依赖NetworkX 3.5和torch 2.9.0。**未明确说明GPU型号、数量或训练时长**。算法本身是CPU实现在工作站上（Q-network较小），可推测未使用GPU训练。

### 5. 实验数量与充分性

- **实验数量**：
  - 3个数据集 × 2种概率设置 × 6个预算值 = 36个主要场景，每个场景比较7种方法（含RL4FPM）。
  - 额外分析了种子集大小（Figure 3）和执行时间（Figure 4）仅针对Uniform概率设置。
  - 无单独的消融实验（如对\(\phi\)、\(\epsilon\)调度、嵌入维度的敏感度分析）。
- **充分性与公平性**：
  - 覆盖了不同规模和结构的真实网络。
  - 对比了6种基线，包括随机、度中心性、PageRank以及公平感知方法（Parity、Crosswalk），具有代表性。
  - 使用固定种子（复现性）和1,000次蒙特卡洛模拟评估利润，统计合理。
  - 未提供标准方差或置信区间，可能影响显著性判断。
  - 缺少对参数（如\(\phi\)、\(\tau\)、\(\epsilon\)衰减速率）的系统性消融研究。

### 6. 论文的主要结论与发现

- RL4FPM在大多数预算下获得最高利润，尤其在中高预算下显著优于所有基线。
  - Uniform设置：在Email-Eu-Core上比最强基线（Random）高3%~25%；在H-BA2k上高60%~501%；在Wiki-Vote上最高提升106%（budget 3000 vs Simple-PageRank）。
  - Trivalency设置：在H-BA2k上利润提升高达836%~978%；在Wiki-Vote上从500到3000预算提升29%~513%。
- 种子集大小分析表明RL4FPM能自适应：在H-BA2k上选更少但更好的种子；在Wiki-Vote上选更多但更便宜的种子；在Email-Eu-Core上选相似数量但质量更高。
- 执行时间：RL4FPM比基线慢2.1~37.6倍，但利润显著更高，体现了时间-性能权衡。

### 7. 优点：方法或实验设计上的亮点

- **问题新颖性**：首次将公平性（最大化最小公平准则）引入利润最大化问题，更具实际意义。
- **方法创新**：利用深度强化学习（DRL）解决组合优化问题，将种子选择建模为MDP，使用Structure2Vec嵌入和边际奖励设计，能够跨图泛化，无需在测试图上重新训练。
- **丰富的对比**：涵盖了随机、中心性、公平感知等多种基线，便于全面评估。
- **深入分析**：除了利润，还分析了种子集大小和执行时间，揭示了RL4FPM的适应性。

### 8. 不足与局限

- **实验覆盖有限**：
  - 仅用了3个数据集，未在更大规模（如百万节点）的网络测试，可扩展性存疑。
  - 未进行消融实验，无法评估各组件（公平权重\(\phi\)、Structure2Vec、\(\epsilon\)调度等）的贡献。
  - 未报告方差，可能掩藏个别运行的不稳定性（如H-BA2k上利润曲线非单调，可能为训练方差所致）。
- **计算代价高**：训练和推理时间远超基线，实际部署可能受限。
- **MDP建模简化**：每步只选一个节点，但种子集质量高度依赖顺序；边际奖励使用单次IC rollout估计，引入噪声。
- **公平定义局限**：最大化最小公平准则可能对分布极不公平的社区过度敏感，其他准则（如比例公平）未探索。
- **未讨论**预算分配、成本-收益函数形式、社区划分来源等现实细节。

（完）
